/**
 * Compatibility entry point for older routes/builds. Keep every photo-homework
 * surface on the single implementation so the automatic page workflow cannot
 * drift from the student or teacher preview.
 */
export { PhotoPageMode, PHOTO_PAGE_FILE_PREFIX } from "./PhotoPageMode";
